# Agent Instructions
- Build private-first media management platform.
- Default all new content to `visibility=private` and `rights_status=personal_only`.
- Never expose permanent public playback URLs.
